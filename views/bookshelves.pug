extends layout

block content
  h1= title
  each site in results
    h3 Name: #{site.name}
    li Material: #{site.material}
    li Number of Shelves: #{site.shelves}
    a(href=`/update/${site._id}` class="btn btn-primary") Update